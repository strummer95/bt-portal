{
  "version": "0.5.3",
  "download_url": "https://raw.githubusercontent.com/strummer95/bt-portal/main/bt-portal-0.5.3.zip",
  "homepage": "https://boomerts.com",
  "tested": "6.7",
  "changelog": "<h4>0.5.3</h4><ul><li>Orders that haven't been paid for no longer read as live exchanges. They show Woo's own wording &mdash; <em>Pending payment</em>, <em>On hold</em> &mdash; as a pill instead of a status dropdown, sit dimmed, and have their own <strong>Unpaid</strong> filter.</li><li>They are out of the Awaiting Items count, so that number is now only exchanges someone is actually waiting on a box for.</li><li>Read from Woo on every load: the moment payment lands, the row rejoins the queue on its own with nothing to click.</li></ul><h4>0.5.2</h4><ul><li><strong>Fix: the portal URL got stuck on quote parameters.</strong> Touching the Quote tab rewrote the address bar to <code>/employees/?qty=4&amp;g=supplied&amp;loc=2</code>, and it stayed there through refreshes and bookmarks. The quote tool syncs the URL on <code>/quote/</code> by design; it is now told not to when it is one tab of the portal.</li><li>Copy Quote Link in the Quote tab now hands you a <code>/quote/</code> link. It was building the link off whatever page it was on, so a quote link copied from here sent the customer to the employee portal.</li><li>Parameters already stuck on the URL are cleared once, on load.</li><li>Needs BT Quote 0.4.1 or newer &mdash; update that one too.</li></ul><h4>0.5.1</h4><ul><li>The seven test orders (4980, 4457, 4456, 4455, 4454, 4453, 4452) are hidden on first load after this update.</li><li><strong>Hide / Restore</strong> on every row &mdash; nothing is deleted, hidden rows move to their own filter and can be brought back. The next test order doesn't need a plugin release.</li><li>Cancelled, refunded and failed orders no longer sit in the pending pile. They get their own filter and a Cancelled pill instead of a status dropdown, so nobody can march a dead order through to Shipped. Read from Woo on every load &mdash; reinstate the order in wp-admin and it rejoins the queue on its own.</li></ul><h4>0.5.0</h4><ul><li>Tab bar is now <strong>Schedule, Online Stores, Quote, Redirect</strong> and an <strong>Other</strong> dropdown. Contacts moved into Other; the pill shows which section you're in and the menu closes on Esc, on selection, or on a click anywhere else.</li><li><strong>New: Exchanges.</strong> Every order containing the Exchange Shipping product, in one list &mdash; order number and link into wp-admin, customer and return address, what they're exchanging for (including whatever the form attached to the line), and the customer's note.</li><li>Each exchange tracks <strong>Awaiting Items &rarr; Received &rarr; Shipped</strong>, plus a return tracking number and a notes field. Saves as you change it, records who changed it, and writes a note onto the WooCommerce order so the history lives with the order too.</li><li>Filter pills across the top with live counts, so \"what's still waiting to come back\" is one click.</li><li>Woo owns the order; the portal only stores where the exchange physically is. Nothing is copied out of Woo, so an order edited in wp-admin is never stale here.</li><li>The Exchange Shipping product ID is set in <em>BT Portal &rarr; Exchanges</em> (default 4441), so the product can be rebuilt in Woo without a plugin release.</li><li>Exchange routes require the portal's wp_rest nonce &mdash; like the order-completion endpoint, and unlike the open job/store routes, since they read customer addresses and write order notes.</li><li><strong>Security fix:</strong> the Contacts tab rendered website form submissions as raw HTML. Anything a stranger typed into the contact form &mdash; including a script tag &mdash; ran in the browser of whichever employee opened the tab. All contact fields are now escaped.</li></ul><h4>0.4.0</h4><ul><li>Every BT plugin now shows the same <strong>Updates</strong> section in the same place &mdash; last on the page, same heading, same button, same wording. Shared file, so they cannot drift apart again.</li><li>Added a Status heading above the portal stats table.</li></ul><h4>0.3.2</h4><ul><li>Art paths now work from a Mac. The board still stores one canonical UNC path (what the btart:// helper on the production PCs opens), but the field accepts whatever anyone pastes and converts it: Finder Copy as Pathname, a dragged file (file:// with %20 spaces), Connect to Server smb:// or afp:// URLs, Explorer's quoted copy-as-path, forward-slash UNC, and mapped drive letters.</li><li>Duplicate mounts are handled &mdash; a share that comes back as BTServer-1 or BTServer 2 resolves to the same server path instead of a dead link.</li><li>Live preview under the Art field shows exactly what the production PC will open, as you type. Green when it resolves cleanly, amber when it's a guess or a mapped drive, red when it's a folder on your own machine that nobody else can reach.</li><li>ART button on the card works on a Mac too &mdash; Macs get an smb:// link into Finder, PCs keep the btart:// helper.</li><li>Server name is one editable line (BT_ART_HOST) instead of being buried in a regex; BT_ART_HOST_BY_SHARE handles any share that lives on a different box.</li><li>Existing saved paths are already canonical and pass through untouched.</li></ul><h4>0.3.1</h4><ul><li><b>FIX: clicking a job card did nothing.</b> BT Quote's submit modal uses <code>id=\"btModalOverlay\"</code> — the same id as the portal's job modal. Since 0.2.5 renders BT Quote's markup inside the Quote tab, two elements shared that id and BT Quote's came first in the document, so <code>btOpenModal()</code> opened <i>its</i> overlay — which lives inside the hidden Quote tab. The click always fired; the modal opened where it couldn't be seen.</li><li>Portal's job modal is now <code>btpJobModalOverlay</code>, and the four class names both plugins shared (<code>bt-modal-overlay</code>, <code>bt-modal-close</code>, <code>bt-modal-body</code>, <code>bt-modal-footer</code>) are <code>btp-</code> prefixed. No ids and no classes overlap between the two plugins now.</li><li>Modal z-index restored to the portal's 999999 (it had been silently inheriting BT Quote's 99999).</li></ul><h4>0.3.0</h4><ul><li>Transfers job cards now have a COMPLETE ORDER button. One click marks the matching WooCommerce order Completed, which fires Woo's own Completed-order email to the customer &mdash; no separate step in wp-admin.</li><li>Confirms first: the button looks the order up and shows order number, customer, email, total, item count and current status before anything is sent. Nothing goes out on a mis-click.</li><li>Order number is resolved server-side from the card itself, never passed in from the browser. Handles 5088, #5088 and prefixed/sequential numbers.</li><li>Won't touch cancelled, refunded or failed orders, and never re-fires the email on an order that is already Completed.</li><li>The card records what happened (woo_order_id, woo_completed_at, woo_completed_by), flips to Complete/Notify Customer, and the button becomes a static ORDER DONE tick so it can't be double-sent.</li><li>This is the one portal endpoint that is not open &mdash; it requires the portal's wp_rest nonce, since it mutates orders and emails customers.</li></ul><h4>0.2.6</h4><ul><li>FIX header: 0.2.5 changed the reset to *:not(.bt-tool):not(.bt-tool *), which raised its specificity to 1,2,0 and made it outrank the header rules, crushing the top bar. Now wrapped in :where(), which contributes zero specificity — reset is back to 1,0,0 and still skips the quote tool.</li><li>FIX quote tool: BT Quote enqueues its CSS and JS from inside its own shortcode; this template never printed them, so the tool rendered as bare HTML with no styling and no JS. The portal now emits both files directly in the tab, in order (CSS, markup, config, JS), and dequeues the copies BT Quote queued so nothing loads twice.</li><li>Still the same files from bt-quote/assets — one tool, one pricing engine, no portal copy.</li></ul><h4>0.2.5</h4><ul><li>The Quote tab is now the exact same tool as the public /quote/ page — BT Quote's [bt_quick_quote], one codebase, no portal copy to drift.</li><li>Two reasons it came through bare before: BT Quote's stylesheet was enqueued mid-content so it landed late or not at all, and the portal reset (#bt-schedule-app *, specificity 1,0,0) outranked every class rule in it. Now the stylesheet is enqueued up front on the portal page, and the reset skips the .bt-tool subtree entirely.</li><li>No spacing rules duplicated into the portal — BT Quote styles its own tool, so the two stay identical automatically.</li></ul><h4>0.2.4</h4><ul><li>Quote tab prices again. Same card, same layout — only the values it sends changed to match BT Quote 0.3.1: garment IDs instead of generic names, \"embroidery\" instead of \"emb\", embType instead of emb_type, retail instead of garment_cost, and it reads perShirt instead of each.</li><li>The garment dropdown is now generated from BT Quote's own pricing tables at render time, so it can never drift from the engine again.</li><li>Max Colors removed — BT Quote prices full colour by location, so the dropdown never changed the number.</li><li>Locations capped at 3 (the engine's limit). Embroidery over the by-quote minimum shows \"By Quote\" instead of an error.</li></ul><h4>0.2.3</h4><ul><li>Quote tab restored to exactly what the snippet port shipped. 0.2.1 (swapping in BT Quote's shortcode) and 0.2.2 (CSS to compensate for it) are both reverted. The portal frontend is byte-identical to v0.2.0 again — the port stays invisible to employees.</li></ul><h4>0.2.2</h4><ul><li><b>Fix:</b> Quote tab layout — the portal's universal CSS reset (<code>#bt-schedule-app *</code>) outranked BT Quote's stylesheet and flattened every margin, padding and gap in the quote tool, collapsing the cards into a run-together mess. The tool's spacing is now re-stated at matching specificity so the reset can't win.</li></ul><h4>0.2.1</h4><ul><li>FIX: Quote tab showed \"Pricing error\" and a plain form. The portal was carrying its own copy of the quote UI from the July 24 port, built against BT Quote v0.1.0. BT Quote is now v0.3.1 and its /price endpoint expects garment IDs (g5000, bc3001, supplied, custom) instead of generic labels (tshirt, hoodie), so every request came back 400 invalid_garment.</li><li>The Quote tab now renders BT Quote's own [bt_quick_quote] shortcode. One quote UI, maintained in one place, no drift.</li><li>Removed 189 lines of duplicated quote code from the portal.</li></ul><h4>0.2.0</h4><ul><li>Redirect module ported in from BT-Sched-5-Redirect — the /stores/ redirect portal and [bt_redirect_tab] now ship with the plugin. Deactivate the old snippet after updating.</li><li>Redirect tab: copy button on each URL Path row copies the full https link to the clipboard.</li><li>All option names, post meta keys, AJAX actions and the shortcode tag are unchanged, so existing redirect pages and click counts carry over untouched.</li></ul><h4>0.1.1</h4><ul><li>Stores tab: copy-to-clipboard icon beside each store's VIEW link.</li></ul><h4>0.1.0</h4><ul><li>Initial plugin release — full port of the BT-Sched WPCode snippets (DB + crons, REST API, [bt_schedule] frontend, head CSS).</li><li>Admin status page with update check, self-updater via GitHub manifest.</li></ul>"
}
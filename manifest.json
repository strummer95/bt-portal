{
  "version": "0.11.1",
  "download_url": "https://raw.githubusercontent.com/strummer95/bt-portal/main/bt-portal-0.11.1.zip",
  "homepage": "https://boomerts.com",
  "tested": "6.7",
  "changelog": "<h4>0.11.1</h4><ul><li>Return Tracking is no longer its own column. The tracking box now sits under <strong>Ship</strong> in the Out column, and pickup rows don't get one &mdash; nothing is in the post.</li></ul><h4>0.11.0</h4><ul><li>New status: <strong>Ready for Pickup</strong>. A row only offers the ending that applies to it &mdash; a pickup exchange can't be marked Shipped, and a shipped one can't be marked Ready for Pickup. Whatever a row is already set to stays in its list, so nothing existing can vanish.</li><li>Moving an exchange in the portal now moves the WooCommerce order with it. Awaiting Items and Received hold it at Processing; Shipped and Ready for Pickup complete it. Cancelled, refunded, failed, unpaid and hidden orders are left alone.</li><li>Ready for Pickup sends the customer its own branded email &mdash; their replacement is finished and waiting in Oswego, with the address. Sends once, and never asks for tracking.</li></ul><h4>0.10.0</h4><ul><li>Two new columns, <strong>In</strong> and <strong>Out</strong>, sitting between New Size and Status. In reads Mailed or Drop off; Out reads Ship or Pickup. Every row carries both, so the two legs can be scanned down the table rather than hunted for.</li><li>Drop off and Pickup are coloured; Mailed and Ship stay grey, so the rows that change what you physically do are the ones that catch the eye.</li><li>Replaces the badges that sat in the Order cell in 0.9.0.</li></ul><h4>0.9.2 &mdash; Redirect works for everyone on the portal</h4><ul><li><strong>Anyone who can open the portal can now use Redirect</strong>, same as every other tab. No WordPress account needed.</li><li>Redirect was the only tab that asked for a WordPress login, and its save/edit/delete endpoints were registered for signed-in users only &mdash; so staff without their own WordPress account couldn't use it whatever their role was.</li><li>If you ever want it held to a higher bar than the rest of the portal, <em>BT Portal &rarr; Redirect access</em> still offers the WordPress-account options.</li></ul><h4>0.9.1</h4><ul><li>Who can use the Redirect tool is now set in <em>BT Portal &rarr; Redirect access</em> &mdash; from any signed-in user up to administrators only. The default is unchanged (Contributor and up), so nobody's access changes until you change it.</li><li>The refusal message now says what to do about it, and tells someone who simply isn't signed in that that's the problem. It used to say only \"You do not have permission to use this tool\", which reads the same either way.</li><li>Signed-out visitors are never let in, whatever the setting.</li></ul><h4>0.9.0</h4><ul><li>Exchanges now show how each one travels. A <strong>Drop off</strong> badge means the customer is bringing the items in; a <strong>Pickup &mdash; do not ship</strong> badge means the finished exchange is collected at the shop and must not go in a mailer. Mail in and ship back is the default and is left unbadged, so only the exceptions catch the eye.</li><li>Read from the order, then from the Exchange Shipping line, then defaulted &mdash; everything placed before customers had the choice reads as mail in, ship back.</li></ul><h4>0.8.1</h4><ul><li>A combined item string keeps its blank positions, so an item with a colour but no size no longer slides the colour up into the Size column.</li></ul><h4>0.8.0</h4><ul><li><strong>Orig Order #</strong> is its own column, with a badge reading <strong>OMG</strong> for a 9-digit number starting 1 and <strong>Chipply</strong> for a 7-digit number starting 8. Neither pattern, no badge — it is left blank rather than guessed.</li><li>The Product cell no longer repeats the order number and School/Team when there are no item rows. Both have their own columns; they were being printed a second time by the raw-meta fallback.</li><li>Exchange details are now read from labelled meta on the Exchange Shipping line as well as from order meta and the old customer note, and the three are merged rather than raced. Item fields are matched by name (<em>Item 1 Product</em>, <em>Item 1 Size</em>, <em>Item 1 Color</em>, <em>Item 1 Qty</em>, <em>Item 1 New Size</em>), or as one combined string, so Size/Color/Qty/New Size fill in as soon as the form writes them — no plugin release needed to match.</li></ul><h4>0.7.4 &mdash; fixes empty tables on /employees/&lt;tab&gt; links</h4><ul><li><strong>Nothing was ever lost.</strong> Tab links stopped WordPress redirecting boomerts.com to www.boomerts.com, so the portal sat on the bare host while the API lived on www &mdash; a different origin, so every call failed and the tables came up blank.</li><li>Host and https corrections are allowed through again; only redirects that would strip the tab off the path are blocked.</li><li>A failed load now says so and says nothing has been lost, instead of showing an empty table.</li></ul><h4>0.7.3</h4><ul><li>Received email now reads \"usually take <strong>up to</strong> 7&ndash;10 days\" &mdash; a ceiling, not a promise.</li></ul><h4>0.7.2</h4><ul><li>The exchange emails are properly Boomer T's now: the logo on a navy header with the pink rule under it, navy section bar, and pink for the new size and the tracking callout. They were using the flat blue off the printed slip with no logo at all.</li><li>Logo URL, navy and pink are set in <em>BT Portal &rarr; Customer email branding</em>, so the brand can move without a plugin release.</li></ul><h4>0.7.1</h4><ul><li>Marking an exchange <strong>Shipped with no tracking number</strong> now stops and asks: <em>No Tracking Number Entered. Are you sure you want to mark this order as shipped and notify the customer?</em></li><li>Saying no saves nothing and puts the dropdown back where it was. A row that already has a tracking number is never questioned.</li></ul><h4>0.7.0</h4><ul><li><strong>Marking Received emails the customer</strong> &mdash; their package arrived, we're on it, we'll ship as soon as we can.</li><li><strong>Marking Shipped emails them the tracking number</strong>, with what went in and what's going out.</li><li>Both are Boomer T's branded and match the printed slip they already have. Replies come back to the shop.</li><li>Set Shipped before you have a tracking number and they still get the heads-up; paste the number in afterwards and they get exactly one follow-up with it. Correcting a number later does not re-mail anyone.</li><li>Nothing sends twice, whatever order the dropdown gets clicked in, and nothing sends on hidden, cancelled, refunded, failed or unpaid orders.</li><li>Every send is written onto the WooCommerce order, and the portal tells you on screen that the customer was mailed.</li></ul><h4>0.6.7</h4><ul><li>Exchanges checks for new orders once a minute <em>while that tab is open</em>. Switch away and the timer is cleared, so no other part of the portal pays for it.</li><li>A new one arriving says so; nothing else on screen moves.</li><li>It holds off while you're typing in a Tracking or Notes box, so a refresh can never wipe a half-entered number, and it skips entirely while the window is in the background &mdash; coming back catches up straight away instead of waiting out the minute.</li><li>Nothing repaints unless something actually changed, so the table doesn't flicker once a minute for no reason.</li></ul><h4>0.6.6</h4><ul><li>School / Team moved inside the item block, so it now reads School / Team &rarr; Product &rarr; Size &rarr; Color &rarr; Qty &rarr; New Size as one group: everything about what is being exchanged, ruled off from who the customer is.</li></ul><h4>0.6.5</h4><ul><li>Product is no longer the widest thing on the screen. It had no width set, so it was soaking up all the leftover space and pushing Size, Color, Qty and New Size across the table away from it.</li><li>Those five now sit together as one tinted, ruled-off block, with a clear break before Status.</li><li>Customer takes the leftover width instead &mdash; the address stops wrapping onto three lines.</li></ul><h4>0.6.4</h4><ul><li>The exchange itself is now real columns: <strong>Product, Size, Color, Qty, New Size</strong> &mdash; the same shape as the form and the printed slip.</li><li>A request with more than one item stacks its values in each of the five columns in the same order, so line two of Product lines up with line two of Size, Color, Qty and New Size.</li><li>Qty is called out when it is more than one.</li><li>An order with nothing structured recorded says so in Product and dashes the rest, rather than faking columns out of a sentence.</li></ul><h4>0.6.3</h4><ul><li>Exchanges now read the request straight off the order (<code>_bt_exchange_items</code>, <code>_bt_original_order</code>, <code>_bt_school_team</code>) instead of reading the note. Exact fields, no guessing at wording.</li><li><strong>School / Team</strong> fills from <code>_bt_school_team</code> as soon as the form sends it.</li><li>Quantity shows when a customer is exchanging more than one of the same thing &mdash; it was being dropped.</li><li>Note parsing stays as a fallback and now understands both wordings, so older orders still display.</li></ul><h4>0.6.2</h4><ul><li>Standing notice on the Quote tab: this page's address is the employee portal, customers can't open it, and the link won't carry the quote &mdash; use <strong>Copy Quote Link</strong>, which makes a boomerts.com/quote link.</li><li>Copying any portal URL out of the page now pops a warning with the public quoter link. Copying ordinary text, or a real /quote/ link, stays silent.</li><li>Copy Quote Link confirms what staff actually need to know &mdash; that the link works for customers.</li><li>Note: nothing on a web page can see a copy made from the browser's address bar. The notice on the tab is what covers that.</li></ul><h4>0.6.1</h4><ul><li>Exchanges is now Order, Customer, School / Team, <strong>Exchanging</strong>, <strong>For</strong>, Status, Return Tracking, Notes, Hide.</li><li>What came in and what goes out are their own columns, rendered in lockstep so a multi-item request stays lined up across both.</li><li><strong>School / Team</strong> column, read from the request note, from order meta, or from meta on the shipping line &mdash; whichever the form ends up using. It will fill in on its own once the field is added to the form; no plugin release needed to match.</li><li>The customer's original order number moved up into the Order cell, next to the Woo order it belongs with.</li></ul><h4>0.6.0</h4><ul><li><strong>Every tab has its own link.</strong> <code>/employees/exchanges</code>, <code>/employees/online-stores</code>, <code>/employees/quote</code>, <code>/employees/redirect</code>, <code>/employees/contacts</code>. Schedule stays on plain <code>/employees/</code>.</li><li>The URL follows as you click, so a refresh keeps you where you were and you can send someone straight to a tab.</li><li>Back and Forward walk through the tabs instead of leaving the portal.</li><li>The route is built from whichever page holds the shortcode, not a hardcoded \"employees\" &mdash; renaming or moving the portal page keeps working.</li><li>If permalinks are plain, it falls back to <code>?tab=exchanges</code> rather than inventing a path that would 404.</li><li><em>After updating, open the portal once.</em> That registers the page and the links go live on the next load. If a deep link 404s, go to Settings &rarr; Permalinks and hit Save once.</li></ul><h4>0.5.5</h4><ul><li>Plugin flags no longer show up as exchange details. <code>is_vat_exempt</code>, <code>tefw_exempt</code> and anything else that is a bare yes/no or a system field are filtered out &mdash; they tell nobody processing an exchange anything.</li><li>Real details still come through, and field names now read as words (<em>Exchange Reason</em>, not <em>exchange_reason</em>).</li><li>If something else creeps in later, it can be added to the <code>btp_exchange_meta_blocklist</code> filter without a release.</li></ul><h4>0.5.4</h4><ul><li>The exchange request is no longer one run-on paragraph. It reads as fields: original order number on its own line, the item ordered with size and colour as chips, and an arrow to what they want instead. Multi-item requests come through as separate blocks.</li><li>If the wording ever changes and the parse fails, the note is still shown in full &mdash; nothing gets swallowed.</li><li>Details attached to the Exchange Shipping line itself, and custom fields on the order, are now shown too. They were being thrown away, which is why some orders looked empty.</li><li>An order with nothing recorded anywhere now says so plainly and links straight into Woo, instead of showing a blank cell.</li><li>Dropped the \"Shipping only\" line &mdash; it was on every single row and said nothing.</li></ul><h4>0.5.3</h4><ul><li>Orders that haven't been paid for no longer read as live exchanges. They show Woo's own wording &mdash; <em>Pending payment</em>, <em>On hold</em> &mdash; as a pill instead of a status dropdown, sit dimmed, and have their own <strong>Unpaid</strong> filter.</li><li>They are out of the Awaiting Items count, so that number is now only exchanges someone is actually waiting on a box for.</li><li>Read from Woo on every load: the moment payment lands, the row rejoins the queue on its own with nothing to click.</li></ul><h4>0.5.2</h4><ul><li><strong>Fix: the portal URL got stuck on quote parameters.</strong> Touching the Quote tab rewrote the address bar to <code>/employees/?qty=4&amp;g=supplied&amp;loc=2</code>, and it stayed there through refreshes and bookmarks. The quote tool syncs the URL on <code>/quote/</code> by design; it is now told not to when it is one tab of the portal.</li><li>Copy Quote Link in the Quote tab now hands you a <code>/quote/</code> link. It was building the link off whatever page it was on, so a quote link copied from here sent the customer to the employee portal.</li><li>Parameters already stuck on the URL are cleared once, on load.</li><li>Needs BT Quote 0.4.1 or newer &mdash; update that one too.</li></ul><h4>0.5.1</h4><ul><li>The seven test orders (4980, 4457, 4456, 4455, 4454, 4453, 4452) are hidden on first load after this update.</li><li><strong>Hide / Restore</strong> on every row &mdash; nothing is deleted, hidden rows move to their own filter and can be brought back. The next test order doesn't need a plugin release.</li><li>Cancelled, refunded and failed orders no longer sit in the pending pile. They get their own filter and a Cancelled pill instead of a status dropdown, so nobody can march a dead order through to Shipped. Read from Woo on every load &mdash; reinstate the order in wp-admin and it rejoins the queue on its own.</li></ul><h4>0.5.0</h4><ul><li>Tab bar is now <strong>Schedule, Online Stores, Quote, Redirect</strong> and an <strong>Other</strong> dropdown. Contacts moved into Other; the pill shows which section you're in and the menu closes on Esc, on selection, or on a click anywhere else.</li><li><strong>New: Exchanges.</strong> Every order containing the Exchange Shipping product, in one list &mdash; order number and link into wp-admin, customer and return address, what they're exchanging for (including whatever the form attached to the line), and the customer's note.</li><li>Each exchange tracks <strong>Awaiting Items &rarr; Received &rarr; Shipped</strong>, plus a return tracking number and a notes field. Saves as you change it, records who changed it, and writes a note onto the WooCommerce order so the history lives with the order too.</li><li>Filter pills across the top with live counts, so \"what's still waiting to come back\" is one click.</li><li>Woo owns the order; the portal only stores where the exchange physically is. Nothing is copied out of Woo, so an order edited in wp-admin is never stale here.</li><li>The Exchange Shipping product ID is set in <em>BT Portal &rarr; Exchanges</em> (default 4441), so the product can be rebuilt in Woo without a plugin release.</li><li>Exchange routes require the portal's wp_rest nonce &mdash; like the order-completion endpoint, and unlike the open job/store routes, since they read customer addresses and write order notes.</li><li><strong>Security fix:</strong> the Contacts tab rendered website form submissions as raw HTML. Anything a stranger typed into the contact form &mdash; including a script tag &mdash; ran in the browser of whichever employee opened the tab. All contact fields are now escaped.</li></ul><h4>0.4.0</h4><ul><li>Every BT plugin now shows the same <strong>Updates</strong> section in the same place &mdash; last on the page, same heading, same button, same wording. Shared file, so they cannot drift apart again.</li><li>Added a Status heading above the portal stats table.</li></ul><h4>0.3.2</h4><ul><li>Art paths now work from a Mac. The board still stores one canonical UNC path (what the btart:// helper on the production PCs opens), but the field accepts whatever anyone pastes and converts it: Finder Copy as Pathname, a dragged file (file:// with %20 spaces), Connect to Server smb:// or afp:// URLs, Explorer's quoted copy-as-path, forward-slash UNC, and mapped drive letters.</li><li>Duplicate mounts are handled &mdash; a share that comes back as BTServer-1 or BTServer 2 resolves to the same server path instead of a dead link.</li><li>Live preview under the Art field shows exactly what the production PC will open, as you type. Green when it resolves cleanly, amber when it's a guess or a mapped drive, red when it's a folder on your own machine that nobody else can reach.</li><li>ART button on the card works on a Mac too &mdash; Macs get an smb:// link into Finder, PCs keep the btart:// helper.</li><li>Server name is one editable line (BT_ART_HOST) instead of being buried in a regex; BT_ART_HOST_BY_SHARE handles any share that lives on a different box.</li><li>Existing saved paths are already canonical and pass through untouched.</li></ul><h4>0.3.1</h4><ul><li><b>FIX: clicking a job card did nothing.</b> BT Quote's submit modal uses <code>id=\"btModalOverlay\"</code> — the same id as the portal's job modal. Since 0.2.5 renders BT Quote's markup inside the Quote tab, two elements shared that id and BT Quote's came first in the document, so <code>btOpenModal()</code> opened <i>its</i> overlay — which lives inside the hidden Quote tab. The click always fired; the modal opened where it couldn't be seen.</li><li>Portal's job modal is now <code>btpJobModalOverlay</code>, and the four class names both plugins shared (<code>bt-modal-overlay</code>, <code>bt-modal-close</code>, <code>bt-modal-body</code>, <code>bt-modal-footer</code>) are <code>btp-</code> prefixed. No ids and no classes overlap between the two plugins now.</li><li>Modal z-index restored to the portal's 999999 (it had been silently inheriting BT Quote's 99999).</li></ul><h4>0.3.0</h4><ul><li>Transfers job cards now have a COMPLETE ORDER button. One click marks the matching WooCommerce order Completed, which fires Woo's own Completed-order email to the customer &mdash; no separate step in wp-admin.</li><li>Confirms first: the button looks the order up and shows order number, customer, email, total, item count and current status before anything is sent. Nothing goes out on a mis-click.</li><li>Order number is resolved server-side from the card itself, never passed in from the browser. Handles 5088, #5088 and prefixed/sequential numbers.</li><li>Won't touch cancelled, refunded or failed orders, and never re-fires the email on an order that is already Completed.</li><li>The card records what happened (woo_order_id, woo_completed_at, woo_completed_by), flips to Complete/Notify Customer, and the button becomes a static ORDER DONE tick so it can't be double-sent.</li><li>This is the one portal endpoint that is not open &mdash; it requires the portal's wp_rest nonce, since it mutates orders and emails customers.</li></ul><h4>0.2.6</h4><ul><li>FIX header: 0.2.5 changed the reset to *:not(.bt-tool):not(.bt-tool *), which raised its specificity to 1,2,0 and made it outrank the header rules, crushing the top bar. Now wrapped in :where(), which contributes zero specificity — reset is back to 1,0,0 and still skips the quote tool.</li><li>FIX quote tool: BT Quote enqueues its CSS and JS from inside its own shortcode; this template never printed them, so the tool rendered as bare HTML with no styling and no JS. The portal now emits both files directly in the tab, in order (CSS, markup, config, JS), and dequeues the copies BT Quote queued so nothing loads twice.</li><li>Still the same files from bt-quote/assets — one tool, one pricing engine, no portal copy.</li></ul><h4>0.2.5</h4><ul><li>The Quote tab is now the exact same tool as the public /quote/ page — BT Quote's [bt_quick_quote], one codebase, no portal copy to drift.</li><li>Two reasons it came through bare before: BT Quote's stylesheet was enqueued mid-content so it landed late or not at all, and the portal reset (#bt-schedule-app *, specificity 1,0,0) outranked every class rule in it. Now the stylesheet is enqueued up front on the portal page, and the reset skips the .bt-tool subtree entirely.</li><li>No spacing rules duplicated into the portal — BT Quote styles its own tool, so the two stay identical automatically.</li></ul><h4>0.2.4</h4><ul><li>Quote tab prices again. Same card, same layout — only the values it sends changed to match BT Quote 0.3.1: garment IDs instead of generic names, \"embroidery\" instead of \"emb\", embType instead of emb_type, retail instead of garment_cost, and it reads perShirt instead of each.</li><li>The garment dropdown is now generated from BT Quote's own pricing tables at render time, so it can never drift from the engine again.</li><li>Max Colors removed — BT Quote prices full colour by location, so the dropdown never changed the number.</li><li>Locations capped at 3 (the engine's limit). Embroidery over the by-quote minimum shows \"By Quote\" instead of an error.</li></ul><h4>0.2.3</h4><ul><li>Quote tab restored to exactly what the snippet port shipped. 0.2.1 (swapping in BT Quote's shortcode) and 0.2.2 (CSS to compensate for it) are both reverted. The portal frontend is byte-identical to v0.2.0 again — the port stays invisible to employees.</li></ul><h4>0.2.2</h4><ul><li><b>Fix:</b> Quote tab layout — the portal's universal CSS reset (<code>#bt-schedule-app *</code>) outranked BT Quote's stylesheet and flattened every margin, padding and gap in the quote tool, collapsing the cards into a run-together mess. The tool's spacing is now re-stated at matching specificity so the reset can't win.</li></ul><h4>0.2.1</h4><ul><li>FIX: Quote tab showed \"Pricing error\" and a plain form. The portal was carrying its own copy of the quote UI from the July 24 port, built against BT Quote v0.1.0. BT Quote is now v0.3.1 and its /price endpoint expects garment IDs (g5000, bc3001, supplied, custom) instead of generic labels (tshirt, hoodie), so every request came back 400 invalid_garment.</li><li>The Quote tab now renders BT Quote's own [bt_quick_quote] shortcode. One quote UI, maintained in one place, no drift.</li><li>Removed 189 lines of duplicated quote code from the portal.</li></ul><h4>0.2.0</h4><ul><li>Redirect module ported in from BT-Sched-5-Redirect — the /stores/ redirect portal and [bt_redirect_tab] now ship with the plugin. Deactivate the old snippet after updating.</li><li>Redirect tab: copy button on each URL Path row copies the full https link to the clipboard.</li><li>All option names, post meta keys, AJAX actions and the shortcode tag are unchanged, so existing redirect pages and click counts carry over untouched.</li></ul><h4>0.1.1</h4><ul><li>Stores tab: copy-to-clipboard icon beside each store's VIEW link.</li></ul><h4>0.1.0</h4><ul><li>Initial plugin release — full port of the BT-Sched WPCode snippets (DB + crons, REST API, [bt_schedule] frontend, head CSS).</li><li>Admin status page with update check, self-updater via GitHub manifest.</li></ul>"
}